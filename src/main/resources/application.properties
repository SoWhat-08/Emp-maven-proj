spring.application.name=employee-management
server.port=8060

# Database configuration maintained by Developer C.
# The application currently uses in-memory data so the project
# can be deployed without requiring a separate MySQL server.
# MySQL configuration can be enabled later:
# spring.datasource.url=jdbc:mysql://localhost:3306/employee_db
# spring.datasource.username=root
# spring.datasource.password=root
